<p>
	<%= link_to "Add contact", new_contact_path, class: "btn btn-primary" %>
</p>
<h1>All Contacts</h1>

<% @contacts.each do |contact| %>
	<h2><%= link_to contact.full_name, contact_path(contact) %></h2>
<% end %>